#!/usr/bin/env python
import wx
import chips.schematix.component_selector
import sys

#Create an application and show the main window
app = wx.App()

frame = wx.Frame(None, size=(1024,768), title="Chips 2.0 Project Browser")
selector = chips.schematix.component_selector.Selector(frame, -1)

frame.Show()
app.MainLoop()
